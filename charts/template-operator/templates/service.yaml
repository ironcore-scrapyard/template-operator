apiVersion: v1
kind: Service
metadata:
  name: {{ include "template-operator.fullname" . }}
  labels:
    {{- include "template-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "template-operator.selectorLabels" . | nindent 4 }}
